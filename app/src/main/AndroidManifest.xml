<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="am.ze.wookoo.myapplication">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FinishActivity"></activity>
        <activity android:name=".ExerciseBeforeActivity" />
        <activity android:name=".TodayActivity" />
        <activity android:name=".TodayBeforeActivity" />
        <activity android:name=".ReadyActivity" />
        <activity android:name=".ReadyBeforeActivity" />
        <activity android:name=".AwardActivity" />
        <activity android:name=".AwardBeforeActivity" />
        <activity android:name=".CalanderActivity" />
        <activity android:name=".CalanderBeforeActivity" />
        <activity android:name=".RecordBeforeActivity" />
        <activity android:name=".ExerciseActivity" />
        <activity android:name=".RecordActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AlarmActivity" />
        <activity android:name=".FavoriteActivity" />
        <activity android:name=".CreateAccountActivity" />

        <receiver android:name=".Alarm_Reciver" />

        <service
            android:name=".RingtonePlayingService"
            android:enabled="true" />
    </application>

</manifest>